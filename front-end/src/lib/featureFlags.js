/**
 * 功能开关
 * ---------------------------------------------------------------
 * 客户定制/评分排名场景下临时停用 AI 研读功能：只保留评分与排名，
 * 不发起任何 AI 网络请求、不渲染 AI 分析区块。评分/排名逻辑与 AI 无关，不受影响。
 *
 * 需要恢复 AI 时，把 AI_ENABLED 改回 true 即可。
 */
export const AI_ENABLED = true;

export default { AI_ENABLED };
